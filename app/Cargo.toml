[package]
name = "footage_viewer"
version = "0.2.0"
edition = "2021"

[dependencies]
footage-viewer-media = { path = "../media" }
eframe = "0.35"
rfd = "0.15"
trash = "5.2.6"

[dev-dependencies]
tempfile = "3"

[build-dependencies]
# Embeds the application icon (icon.ico) into the Windows executable.
winresource = "0.1"
